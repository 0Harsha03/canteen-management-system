# MongoDB Configuration (Use MongoDB Atlas for production)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/canteen-management

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8081

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-here
JWT_EXPIRE=30d

# Admin Configuration (for initial admin user)
ADMIN_EMAIL=admin@canteen.com
ADMIN_PASSWORD=admin123